info:
  icon: square us
  name: UnixTimeStamp
  description: UnixTimestamp is an extension used to convert Unix timestamps into dates.
actions:
  - meta:
      title: UnixTimeStamp
      icon: square us
      identifier: selected.parse_uts
      after: "show"
      regex: "[0-9]+"
    runCommand:
      command:
        - /bin/zsh
        - parse.sh
